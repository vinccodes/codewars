function switcher(x){
    // TODO: convert each item to its unicode 
    
    // loop through each element
    for (let i =0; i < x.length; i ++){
      // convert element to string 
      console.log(String.fromCharCode(x[i]));
      
      // join the string together
    }
    
    // return as string
  }